actiavr entorno virtual
.\.venv\Scripts\Activate.ps1


requirements installation: dbe estar dentro de backend el txt
pip install -r requirements.txt


ejecutar:
uvicorn app.main:app --reload



cuando no encuntra tal modulo app, hay que:
cd backend
uvicorn app.main:app --reload

